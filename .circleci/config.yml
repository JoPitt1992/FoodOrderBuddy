jobs:
  build:
    executor: docker-executor
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true

      - run:
          name: Build JAR (Production)
          command: mvn clean package -DskipTests -Pproduction ...

      - run:
          name: Install gcloud
          command: |
            sudo apt-get update && sudo apt-get install -y apt-transport-https ca-certificates gnupg curl
            curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | \
              gpg --dearmor | \
              sudo tee /usr/share/keyrings/cloud.google.gpg > /dev/null
            echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] https://packages.cloud.google.com/apt cloud-sdk main" | \
              sudo tee /etc/apt/sources.list.d/google-cloud-sdk.list
            sudo apt-get update && sudo apt-get install -y google-cloud-sdk

      - run:
          name: Auth GCP
          command: |
            echo $GCLOUD_SERVICE_KEY | base64 -d > ${HOME}/gcloud.json
            gcloud auth activate-service-account --key-file=${HOME}/gcloud.json
            gcloud config set project foodorderbuddy
            gcloud auth configure-docker europe-west1-docker.pkg.dev

      - run:
          name: Build & Push Docker Image
          command: |
            docker build -t europe-west1-docker.pkg.dev/foodorderbuddy/foodorderbuddy/foodorderbuddy-app:latest .
            docker push europe-west1-docker.pkg.dev/foodorderbuddy/foodorderbuddy/foodorderbuddy-app:latest

  deploy:
    executor: docker-executor
    steps:
      - run:
          name: Decode GCP Key
          command: echo $GCLOUD_SERVICE_KEY | base64 -d > ${HOME}/gcloud.json

      - run:
          name: Install gcloud
          command: |
            sudo apt-get update && sudo apt-get install -y apt-transport-https ca-certificates gnupg curl
            curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | \
              gpg --dearmor | \
              sudo tee /usr/share/keyrings/cloud.google.gpg > /dev/null
            echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] https://packages.cloud.google.com/apt cloud-sdk main" | \
              sudo tee /etc/apt/sources.list.d/google-cloud-sdk.list
            sudo apt-get update && sudo apt-get install -y google-cloud-sdk

      - run:
          name: Deploy to Cloud Run
          command: |
            gcloud auth activate-service-account --key-file=${HOME}/gcloud.json
            gcloud config set project foodorderbuddy
            gcloud run deploy foodorderbuddy \
              --image=europe-west1-docker.pkg.dev/foodorderbuddy/foodorderbuddy/foodorderbuddy-app:latest \
              --region=europe-west1 \
              --platform=managed \
              --allow-unauthenticated
